{
  "name": "@kimi-next/model",
  "version": "0.0.1",
  "private": true,
  "description": "Model profiles, capabilities, and catalog",
  "license": "MIT",
  "type": "module",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "refresh-catalog": "node scripts/refresh-catalog.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {"@kimi-next/ir": "workspace:*"}
}
