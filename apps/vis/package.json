{
  "name": "@moonshot-ai/vis",
  "version": "0.1.1",
  "private": true,
  "description": "Debug visualization tool for kimi-code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "bun run --cwd ../../packages/agent-core-v2 build",
    "predev": "bun run build:deps",
    "dev": "node scripts/dev.mjs",
    "build": "bun run build:deps && bun run --filter @moonshot-ai/vis-server build && bun run --filter @moonshot-ai/vis-web build && node scripts/copy-web-dist.mjs",
    "prestart": "bun run build",
    "start": "node server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
